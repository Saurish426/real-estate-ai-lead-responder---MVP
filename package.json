{
  "name": "real-estate-ai-lead-responder-mvp",
  "version": "1.0.0",
  "description": "Backend foundation for the Real Estate AI Lead Responder MVP.",
  "main": "src/server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "build": "npm run prisma:generate",
    "prisma:generate": "prisma generate",
    "db:deploy": "prisma migrate deploy",
    "deploy:prepare": "npm run prisma:generate && npm run db:deploy",
    "verify:deployment": "node src/scripts/verifyDeployment.js",
    "capture:gmail-test": "node src/scripts/captureTestGmailLead.js"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.8.0",
    "@prisma/client": "^7.8.0",
    "express": "^4.19.2",
    "imapflow": "^1.3.3",
    "mailparser": "^3.9.8",
    "nodemailer": "^8.0.7",
    "pg": "^8.20.0"
  },
  "devDependencies": {
    "prisma": "^7.8.0"
  }
}
